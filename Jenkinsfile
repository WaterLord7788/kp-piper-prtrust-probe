@Library('piper-lib-os') _

// The documented Piper usage. Everything about which stages may run is supposed to be
// decided here and by the library defaults, not by the revision being built.
piperPipeline script: this
